---
  - name: Desable accounts 
    hosts: all
    become: yes
    become_method: sudo
    tasks:
      - name: Disable user account
        command: "lastlog -b 90 | awk '!/Never log/ {if (NR > 1) print $1}' |  xargs -I{} usermod -L {}"
